// VectorMemory 不再使用 PostgreSQL 存储向量
// 向量数据（embedding + metadata）统一由 Upstash Vector 管理
//
// 在 Upstash Vector 中，每条记录的结构为：
//   id:       string          — 唯一标识，建议格式 "{novelId}:{type}:{uuid}"
//   data:     string          — 原始文本（Upstash 使用 BGE-M3 自动生成 embedding）
//   metadata: {
//     novelId:   number       — 所属小说 ID
//     type:      string       — "chapter_summary" | "event" | "character_memory"
//     content:   string       — 原始文本内容（冗余存储，方便检索后直接使用）
//     chapterId: number?      — 关联章节 ID（可选）
//     ...其他自定义字段
//   }
//
// 查询示例（通过 VectorDBService）：
//   vectorDBService.query("主角与反派的第一次交锋", {
//     topK: 5,
//     filter: "novelId = 42 AND type = 'event'"
//   })
